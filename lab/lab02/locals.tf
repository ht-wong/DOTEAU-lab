# DONE
locals {
  revision = var.revision
  # ############################# Task 1 ##################################
  /*
  app             = 
  env             = 
  # 构建类似 /[app]/[env]
  resource_prefix = join("/", [local.app, local.env])
  
  network_components          = ["vpcs", "subnets",  ...]
  network_network_console_url = 
  */
}